import ActionType from './action/ActionType';

const initState = {
  regions: [],
  categories: [],
  restaurants: [],
};

export default function reducer(state = initState, action) {

  return state;
}
